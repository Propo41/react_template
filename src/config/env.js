/**
 * This file is used to configure the environment variables for the application.
 * All environment variables are to be exported through this file.
 *
 * For example, if you want to use the environment variable `REACT_APP_PORT`, you can
 * export it as follows:
 * $ export const PORT = process.env.REACT_APP_PORT;
 */
export const NODE_ENV = process.env.NODE_ENV || "development";
